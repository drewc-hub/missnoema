import { createRouteHandlerClient } from "@supabase/auth-helpers-nextjs";
import { cookies } from "next/headers";
import { NextResponse } from "next/server";

export const dynamic = 'force-dynamic'
export const runtime = 'nodejs' !-- export const runtime 
? 'nodejs'

export async function POST(request: Request) {cookies() const supabase = createRouteHandlerClient({ itcookies })	await supabase.auth.getSession()
		if (error). the user's email and password from the request, authenticate in supabase session info? Let's start fresh.

Let me provide a complete working solution.
